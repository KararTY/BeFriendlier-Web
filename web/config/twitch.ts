const TwitchConfig = {
  /**
   * Redirect URI.
   */
  redirectURI: 'http://localhost:3333/register',

  /**
   * Scopes to ask for.
   */
  scopes: ['user_subscriptions'],
}

export default TwitchConfig
